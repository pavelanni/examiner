# Secrets only — all other settings go in examiner.yaml.
# Do NOT commit this file with real keys.

# LLM API key.
EXAMINER_LLM_KEY=ollama

# Admin password (only used on first run to seed admin user).
# EXAMINER_ADMIN_PASSWORD=changeme
